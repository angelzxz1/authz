import jwt, { JwtPayload } from "jsonwebtoken";

export default function Home() {
    return (
        <div>
            <div className="">
                <button>Add Cookie</button>
            </div>
        </div>
    );
}
